/*console.log("hola");

const hola = document.getElementById('div');

/*hola.innerHTML= `hola`;
hola.addEventListener('click' ,function(){
    hola.innerHTML=`chau`;
})
*//*
const divC = document.getElementById('divC');

divC.addEventListener('click',function(){
    divC.classList.add('divC');
});
divC.addEventListener('mouseenter', ()=>{
    divC.classList.add('red');
})
divC.addEventListener('mouseleave', ()=>{
    divC.classList.remove('red');
    divC.classList.add('blue')
})
*/

/*const parent = document.getElementById('parent')

// console.log(parent.parentElement)

console.log(parent.childNodes);


console.log(parent.children)*/

const list = document.getElementById('list');
const newElement = 'Soy un nuevo elemento';

// list.children[0].before(newElement);
// list.prepend(newElement);
// list.append(newElement);
// list.children[0].after(newElement);
// list.children[0].replaceWith(newElement)


// console.log(list.cloneNode(true));
// list.after(list.cloneNode(true));

// list.remove();
// list.removeChild(list.children[0]);